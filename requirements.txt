pytest>=8.0
pandas>=2.0
requests>=2.31
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
numpy>=1.26

# opencv-python-headless is used here because it's what test/CI environments
# need (no GUI/display libraries required). If you want to also use
# cv2.imshow() interactively while working through Week 11 on your own
# machine, install the regular GUI-enabled package instead:
#     pip uninstall opencv-python-headless
#     pip install opencv-python
opencv-python-headless>=4.9

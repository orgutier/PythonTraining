"""
Functions -- Recursion Basics
Implement the function(s)/class(es) below.

IMPORTANT: do not add an `if __name__ == "__main__":` block to this file.
It must stay a plain importable module -- the test suite in
tests/test_stage03_exercise01.py imports directly from here. Need a helper
function or class of your own? Add another .py file next to this one inside
exercises/stage03/exercise01/ and import it as a submodule (e.g.
`from exercises.stage03.exercise01 import helpers`) -- solution.py just has to
stay the entry point these tests import from.
"""


def factorial(n: int) -> int:
    """n! -- MUST be implemented recursively. Raise ValueError if n < 0."""
    raise NotImplementedError


def fibonacci(n: int) -> int:
    """nth Fibonacci number (0-indexed) -- MUST be implemented recursively."""
    raise NotImplementedError

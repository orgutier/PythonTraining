"""
Python Fundamentals -- Marathon Pace Report
Write plain top-level code below -- no function/class wrapper needed (or expected) for this one. Assign your answers to the exact variable names named in README.md; the tests import this module and read those variables directly.

IMPORTANT: do not add an `if __name__ == "__main__":` block to this file.
It must stay a plain importable module -- the test suite in
tests/test_stage01_tier2_mid02.py imports directly from here. Need a helper
function or class of your own? Add another .py file next to this one inside
exercises/stage01/tier2_mid02/ and import it as a submodule (e.g.
`from exercises.stage01.tier2_mid02 import helpers`) -- solution.py just has to
stay the entry point these tests import from.
"""


race_distance_km_text = "42.195"
elapsed_minutes_text = "255"
target_minutes_text = "240"

raise NotImplementedError  # delete this line once you've written the code below

# Write your code here: cast the three values above, then compute
# pace_min_per_km, pace_report (walrus + f-string), minutes_over_per_km,
# on_pace (chained comparison), total_penalty_seconds (+=, twice), and
# fatigue_index (precedence-sensitive). See README.md.

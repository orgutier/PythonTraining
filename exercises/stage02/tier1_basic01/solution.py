"""
Control Flow -- Warehouse Grid Scanner
Write plain top-level code below -- no function/class wrapper needed (or expected) for this one. Assign your answers to the exact variable names named in README.md; the tests import this module and read those variables directly.

IMPORTANT: do not add an `if __name__ == "__main__":` block to this file.
It must stay a plain importable module -- the test suite in
tests/test_stage02_tier1_basic01.py imports directly from here. Need a helper
function or class of your own? Add another .py file next to this one inside
exercises/stage02/tier1_basic01/ and import it as a submodule (e.g.
`from exercises.stage02.tier1_basic01 import helpers`) -- solution.py just has to
stay the entry point these tests import from.
"""


grid_rows = ["..#..", ".##..", "?.#.#", "?????", "#..X."]

raise NotImplementedError  # delete this line once you've written the code below

# Write your code here: nested for-loops (with continue/pass) for
# obstacle_count/unmapped_count/invalid_char_count/first_obstacle_row/
# first_obstacle_col/rows_skipped, then a separate while-loop (with break)
# for last_obstacle_row. See README.md for the exact requirements.

"""
Control Flow -- Number Patterns
Implement the function(s)/class(es) below.

IMPORTANT: do not add an `if __name__ == "__main__":` block to this file.
It must stay a plain importable module -- the test suite in
tests/test_stage02_exercise01.py imports directly from here. Need a helper
function or class of your own? Add another .py file next to this one inside
exercises/stage02/exercise01/ and import it as a submodule (e.g.
`from exercises.stage02.exercise01 import helpers`) -- solution.py just has to
stay the entry point these tests import from.
"""


def fizzbuzz(n: int) -> list[str]:
    """Return ["1", "2", "Fizz", "4", "Buzz", ...] for 1..n."""
    raise NotImplementedError


def is_prime(n: int) -> bool:
    """Return True if n is prime. Implement with for...else (see README)."""
    raise NotImplementedError
